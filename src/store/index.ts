import { createStore } from "vuex";
// import { IForm } from '../utils/formInterface';

export default createStore({
  state: {
    formData: {}
  },
  mutations: {},
  actions: {},
  modules: {}
});
